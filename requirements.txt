# Django framework
Django>=5.1  

# Web server for production (optional, but recommended)
gunicorn  

# Serving static files efficiently
whitenoise  

# Database configuration helper (especially if switching to PostgreSQL later)
dj-database-url  

# Environment variable management (if using a .env file)
python-dotenv  

# Mathematical and scientific libraries needed for mesh generation
numpy  
scipy  
matplotlib  

